{
  "name": "qurantab-landing-page",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run build && git subtree split --prefix dist main -b gh-pages-deploy && git push origin gh-pages-deploy:gh-pages --force && git branch -D gh-pages-deploy",
    "preview": "vite preview",
    "lint": "eslint src/js/",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.1.0",
    "globals": "^17.4.0",
    "prettier": "^3.8.1",
    "sass": "^1.98.0",
    "vite": "^8.0.3"
  }
}
